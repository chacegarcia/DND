# Workbook snapshot (product data); keep local only — not for public/game repo
db/data.json

# Optional esbuild IIFE (npm run build:dungeon-bundle); not source — do not commit
js/dungeon-suso-bundle.iife.js

node_modules/
.DS_Store
